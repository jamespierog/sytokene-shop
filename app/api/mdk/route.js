// This single route handles all MoneyDevKit communication —
// checkout creation, invoice generation, payment verification.
// The SDK does everything; you just export the POST handler.
export { POST } from "@moneydevkit/nextjs/server/route";
